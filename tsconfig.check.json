// The build config excludes tests so they stay out of dist, and vitest
// transpiles without checking, so nothing type-checks them without this.
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
